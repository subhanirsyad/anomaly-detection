# Data folder

Taruh dataset event log kamu di sini.

Disarankan nama file:
- `clue.jsonl`

Format: JSON Lines (1 event per baris), minimal ada field `uid`, `time`, `type`.

Jika nama kolom berbeda, lihat fungsi normalisasi di notebook dan sesuaikan.
